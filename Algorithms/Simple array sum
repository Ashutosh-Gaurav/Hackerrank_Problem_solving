#include<bits/stdc++.h>
using namespace std;

int main()
{
    int N,sum=0;
    cin>>N;
    int arr[N];

    for(int i=0;i<N;i=i+1)
    {
        cin>>arr[i];
        sum+=arr[i];
    }
    cout<<sum;
    return 0;
}
