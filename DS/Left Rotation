#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n,d;
    cin>>n>>d;
    vector<int> arr;
    for (int i=0;i<n;i++)
    {
        int temp;
        cin>>temp;
        arr.push_back(temp);
    }
    rotate(arr.begin(),arr.begin()+d,arr.end());
    for (int i=0;i<n;i++)
    {
       cout<<arr[i]<<" ";
    }
    return 0;
}

